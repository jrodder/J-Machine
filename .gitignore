__pycache__/
*.pyc
.venv/
j_machine.egg-info/
zmach-save.bin

# conformance-test save artifacts
*.mem
*.mem.aux

# runtime host state (spec §3/§5): RNS identities (PRIVATE KEYS),
# player save slots, ratchets, announce cache — operator-owned, never
# versioned. Back up data/ separately (tarball) if it matters.
data/
